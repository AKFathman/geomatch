# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
next-env.d.ts

# Env / secrets
.env
.env.local
.env.*.local
*.pem

# ETL build artifacts (regenerated each run)
/etl/output/
/etl/cache/
*.duckdb

# NOTE: web/public/data/ is intentionally NOT ignored — it's how we ship
# the feature matrix to the frontend. The nightly ETL writes its parquet
# output into web/public/data/feature_matrix.parquet and commits it.

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
.cursor/
